const Title = ({ title }) => {
  return (
    <section>
      <h1 className="text-[2rem] font-manuscript text-center text-pink">
        {title}
      </h1>
    </section>
  );
};
export default Title;
